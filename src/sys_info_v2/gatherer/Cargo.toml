[package]
name = "gatherer"
version = "0.3.0"
edition = "2021"

[dependencies]
anyhow = { version = "1.0" }
interprocess = { version = "1.2" }
libc = { version = "0.2" }
raw_sync = { version = "0.1" }
shared_memory = { package = "shared_memory_extended", version = "0.13" }

[profile.release]
opt-level = 3
lto = true
